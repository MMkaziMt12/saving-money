
"use client";

// This component is no longer needed as its functionality is replaced by
// the Sidebar component from "@/components/ui/sidebar"
// and managed by SidebarProvider in src/app/(app)/layout.tsx.
// You can safely delete this file.

// To prevent build errors if it's still imported somewhere temporarily,
// I'll leave a minimal export.

export function AppSidebar() {
  return null;
}
